# database I'm using "neon" https://neon.tech
DATABASE_URL=***********
DATABASE_SECRET=***********
DRIZZLE_DATABASE_URL=postgresql://***************

#auth
AUTH_SECRET="any_random_key"
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Capacitor mobile dev (localhost on a phone = the phone, NOT your PC)
# Use npm run cap:dev:device or cap:dev:emulator instead of raw localhost.
# CAPACITOR_TARGET=device|emulator|ios-simulator
# CAPACITOR_SERVER_URL=http://192.168.1.10:3000
# CAPACITOR_DEV_PORT=3000
